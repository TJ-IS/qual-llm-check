---
otero_id: 21143
otero_key: "99M75GUN"
title: "Model composition in a distributed environment"
authors: "Kaushal Chari"
year: "2003"
journal: "Decision Support Systems"
doi: "10.1016/s0167-9236(02)00116-1"
query: "construct"
source: "https://ais.kexu.win"
images_downloaded: false
---
www.elsevier.com/locate/dsw

# Model composition in a distributed environment

Kaushal Chari

Information Systems/Decision Sciences, University of South Florida, 4202 East Fowler Avenue, CIS 1040, Tampa, FL 33620-7800, USA

Received 1 February 2002; accepted 1 April 2002

## Abstract

Organizations that operate from multiple locations have data and model resources that are distributed at various sites of the organization. Decision making is facilitated when these resources are leveraged to support model composition and execution, i.e., composing and executing a sequence of models in response to a particular decision making situation. In this paper, we address the problem of model composition when data sources (models and data) are distributed across multiple sites and have different scopes.

<sup>D</sup> 2002 Elsevier Science B.V. All rights reserved.

Keywords: DSS; Model management; Model composition; Model integration; Distributed systems

## 1. Introduction

Two important resources that aid users in managerial decision making within an organization are models and data (note that we view a ‘‘model’’ as a computer-executable procedure that may require data inputs, a view that is widely held in the decision support systems (DSS) literature [7,27,30]). Examples of model include: a demand-forecasting program that uses price data to forecast product demands, a production planning program based on linear programming that determines the optimal quantities of products to be produced, etc. Models and data are typically distributed at various organizational sites on a variety of platforms that are interconnected by a communications network. This environment is characterized as distributed and heterogeneous.

One of the challenging issues facing organizations today is to leverage existing data and model resources for decision making. This entails composing and executing a sequence of models (i.e., a composite model) on different platforms while acquiring model input data from databases at multiple locations. In practice, data available in databases are often stored at different levels of some generalization/specialization (i.e., data scope) hierarchy. Model composition complexity increases when data have differing scopes, and when data is distributed with overlapping key values among database fragments. The scope of an input data of a model may not be the same as the scope of data stored in databases. For example, a cost model may require the inventory count of a particular product across all plants in an organization as input, whereas the inventory count may be stored in database tables on a plant-byplant basis, thereby requiring aggregation of data before it is presented to the model as input.

In this paper, we address the following problem. Given a query and a set of data sources (i.e., database tables and model executables) distributed at various sites with overlapping key values and having different scopes. How could data sources be integrated to form composite models and then be executed in response to the query with minimal human intervention?

![](/api/attachments/99M75GUN/fulltext/images/dcc1683a84b76950eb2fdcc4d6c0d7a9963fe00d97fadeafa8797eba542d0ea2.jpg)  
Fig. 1. A model composition sequence.

## 1.1. An example

We present a hypothetical organization to illustrate model composition and execution in the context of differing data scopes and when data is distributed with overlapping key values. Consider ABC Tool, an organization that operates three plants in US that are located in California (CA), Florida (FL), and Virginia (VA), respectively, and two plants in Canada that are located in British Columbia (BC) and Ontario (ON), respectively. Each plant site has a computer that contains relational database tables and models (that are executable programs). Fig. 1 illustrates the integration of the production model and the sales forecasting model to answer a query that requires the total quantity of all products to be produced by the US operations. The production model, which determines the total units of various products to be produced in a production cycle, uses inventory level (given by quantity) and projected sales as inputs. The projected sales is generated as an output by the sales-forecasting model, whereas inventory level is generated by summing the quantity attribute grouped by products across three database tables VAProd, CAProd, and FLProd, respectively (Tables 1– 3). These tables, which are located in New York, Virginia, and Texas, respectively, contain data on items produced at their respective locations (Similar tables exist for Canadian plants: BCProd and ONProd). As seen from Tables 1 and 2, products with the same product identifier (pid), i.e., pid = 101 and 102 are produced at two locations, thus overlapping key values exists in these tables. There could be replication of data as well. The primary copy of VAProd is stored in Virginia whereas a secondary copy may be maintained in California. The database tables of ABC can be viewed in the context of a data scope hierarchy as shown in Fig. 2. A query requiring total inventory level of all products for the entire US operations would involve retrieving quantity data from VAProd, CAProd and FLProd and then aggregating them on a product-by-product basis.

Relational table VAPROD containing data on products produced at the VA plant

<table><tr><td>Pid</td><td>Pname</td><td>Price</td><td>Quantity</td><td>Unit_cost</td></tr><tr><td>101</td><td>Washer-1/4</td><td>4.00</td><td>100</td><td>2.00</td></tr><tr><td>102</td><td>Washer-1/2</td><td>6.00</td><td>150</td><td>3.00</td></tr></table>

Relational table CAPROD containing data on products produced at the CA plant

<table><tr><td>Pid</td><td>Pname</td><td>Price</td><td>Quantity</td><td>Unit_cost</td></tr><tr><td>101</td><td>Washer-1/4</td><td>4.00</td><td>100</td><td>2.00</td></tr><tr><td>102</td><td>Washer-1/2</td><td>6.00</td><td>150</td><td>3.00</td></tr><tr><td>103</td><td>Bolt-2</td><td>8.00</td><td>50</td><td>4.00</td></tr><tr><td>104</td><td>Bolt-3</td><td>10.00</td><td>50</td><td>5.00</td></tr></table>

Relational table FLPROD containing data on products produced at the FL plant

<table><tr><td>Pid</td><td>Pname</td><td>Price</td><td>Quantity</td><td>Unit_cost</td></tr><tr><td>105</td><td>Hinge-1/2</td><td>12.00</td><td>50</td><td>6.00</td></tr><tr><td>106</td><td>Hinge-1</td><td>14.00</td><td>50</td><td>7.00</td></tr></table>

Consider a query that requires the total quantity of product with pid = 102 to be produced by the entire operations of ABC (that includes US and Canadian plants). Here the scope of the query is the root scope. The same composite model as in Fig. 1 is used, however, model input data processing is far more complex. First, price data for product with pid = 102 are retrieved from VAProd, CAProd, FLProd, BCProd and ONProd, then averaged before sending it to the sales forecasting model. The inventory level input is generated by aggregating quantity field value from all tables VAProd, CAProd, FLProd, BCProd and ONProd for pid = 102, since the scope of the query is the root scope. Data is retrieved from database copies nearest to the user.

Many proposals on model composition and execution are available in the literature [1 – 3,5 – 7,12 – 18,21,23,22,25]. However, only few proposals deal with model composition and execution in a distributed environment [1,5,13,14,21]. To our knowledge, the only proposal that exists in the model composition literature that deals with data scopes is by Dutta and Basu [12]. Dutta and Basu propose a sort hierarchy in their first-order logic approach for unifying a variable of a given sort with terms at the same level or lower in the sort hierarchy. They do not address the issue of arithmetic computations based on the hierarchy.

Few papers from the heterogeneous database literature have addressed data integration in a heterogeneous environment [4,19,20,26,29,31,32]. These papers primarily deal with the differences in data representation and storage as well as with the differences in the underlying platforms during data integration. Various data models such as those based on object-oriented concepts [4,19,31,32], knowledgebased approaches [20], etc. have been proposed to represent global schemata of federated database systems. To our knowledge, none of the proposals in the heterogeneous database systems literature address the issue of data scopes. In the data warehousing literature, differences in the data scopes are handled using the aggregation operator during roll-up [11], i.e., when the scope of a query is at a higher level than the scope of the data stored. However to our knowledge, none of the proposals in the data warehousing literature address automation of scope matching and roll-up processing based on some user-specified ad hoc query.

![](/api/attachments/99M75GUN/fulltext/images/41d7a9cd79e04755a39bc3b150a58f0a066a75f4867882b3ef3fd1a573ebd033.jpg)  
Fig. 2. A data scope hierarchy.

The contributions of this paper are as follows. First, we present constructs to support model composition and execution when data have differing scopes and when data are distributed over multiple platforms with overlapping key values. These constructs, which are developed by extending prior research on filter spaces [9], are used to represent metadata of models and data. A loose collection of the metadata of models and data constitutes the global schema in our approach. A control procedure uses the metadata and associated filter spaces to create and execute composite models. In contrast to the federated database systems, where significant effort is used to integrate various local schemata to create a global schema, in our approach, very less effort is expended in creating a global schema since it is a loose collection of metadata. Therefore, metadata of models and data can be easily added or removed from the global schema. Furthermore, the constructs presented enable model composition to be independent of the underlying implementation and the distribution of models and data. We then present a systems architecture that (1) enables model and data implementation and distribution to be transparent to the user, (2) supports a high degree of automation during model composition and execution, and (3) enables the use of a variety of platforms, access methods and protocols. In contrast to our approach, OLAP tools from the data warehousing literature do not have the ability for automating scope matching and related processing to handle ad hoc queries. Furthermore, OLAP tools cannot handle data distribution. The constructs and the architecture presented in this paper, jointly facilitate a solution to the research problem in this paper.

This paper is divided into six sections and an appendix. Section 2 provides an informal description of the filter space approach for model composition. Section 3 contains formal specifications of the data scope hierarchy. Section 4 contains details of model composition. Description of the systems architecture is presented in Section 5. The contributions of this paper are summarized in Section 6. Appendix A contains definitions related to the filter space approach.

## 2. The filter space approach for model composition

A filter space is an n-dimensional space that represents the space defined by constraints known as filter clauses. A group of filter clauses constitutes a filter list. A filter list can be associated with a data source (i.e., a relational table or a model output expressed as a relation) to specify the logical conditions (i.e., extent) of the data in the data source. The filter space of a data source encloses all the tuples of the data source. Model composition entails matching filter spaces of queries with the filter spaces of model outputs. In this section, we present an informal description of the model composition approach using filter spaces. Formal definitions pertaining to the filter space approach from [9] are presented in Appendix A for the sake of completeness.

A filter list contains filter clauses in the conjunctive normal form [24]. There are two generic types of filter clauses: Type 1 and Type 2. Type 1 filter clauses in turn consist of two subtypes: Type 1a and Type 1b. In general, Type 1 filter clauses are clauses of two places $\varPhi ( \alpha _ { 1 } , \alpha _ { 2 } )$ where $\Phi { \in } \{ \mathrm { L T } ( \mathrm { < } )$ , LE(< =), GT(>), GE(>=)}. In the case of a Type 1a filter clause, $\alpha _ { 1 }$ and $\alpha _ { 2 }$ represent variables. An example of Type 1a filter clause is GE(price, unit<sup>\_</sup>cost) that represents the logical condition: price <sub>z</sub> unit<sup>\_</sup>cost. In the case of a Type 1b filter clause, $\alpha _ { 1 }$ represents a variable, and $\alpha _ { 2 }$ is a constant such that MIN<sup>\_</sup>RANGE V a V MAX<sup>\_</sup> RANGE, where MIN<sup>\_</sup>RANGE and MAX<sup>\_</sup>RANGE are low and high values, respectively, that are never taken by $\alpha _ { 1 }$ . An example of a Type 1b filter clause is GE(pid, 101) that represents the logical condition: pid <sub>z</sub> 101.

Type 2 filter clauses are of the form $\mathrm { O R } ( \alpha _ { 1 } , . . . . , \alpha _ { n } )$ $n { > } 0 ,$ , where the predicate symbol represents disjunction and $\mathcal { \alpha } _ { 1 } , . . . , \mathcal { \alpha } _ { n }$ are predicates of Type 1. An example of a Type 2 clause is OR(GE(pid,101),LE(pid, 999)) that represents the condition $( \mathrm { p i d } \geq 1 0 1 ) \land ( \mathrm { p i d } \leq 9 9 9 )$ . The filter list [GE(price, unit<sup>\_</sup>cost), OR(GE(pid, 101), LE(pid, 999))] represents the conjunction of its component filter clauses and is equivalent to (price <sub>z</sub> unit $\mathcal { . } \mathrm { c o s t ) } \vee ( ( \mathrm { p i d } \geq 1 0 1 ) \wedge ( \mathrm { p i d } \leq 9 9 9 ) )$ .

A filter list can be represented in an n-dimensional space, where the dimensions are given by the set of unique variables in the filter list. The feasible region in the n-dimensional space that satisfies all the logical conditions in the filter list is the filter space. Readers are referred to Appendix A for a formal definition of a filter space. The filter space for the filter list $\pi { = } [ \mathrm { G E } ( \mathrm { p i d } ,$ 101), LE(pid, 103), GE(cost, 10.0), LE(cost, 20.0)] is illustrated in Fig. 3. Note that this is a two-dimensional space with dimensions pid and cost. Tuples involving all combinations of (pid, cost) values that enable all filter clauses in p to be true are enclosed in the filter space of p.

Matching logical conditions of a query to the logical conditions representing a data source involves matching filter spaces. The matching problem is complicated by the fact that the dimensions of the filter spaces being matched are not the same. Hence, filter lists have to be transformed in a logically consistent manner so that the dimensions of the filter spaces being compared are equal. This transformation is accomplished using a projection operation (see Definition VII in Appendix A). The following example illustrates the transformation. Consider two filter lists: $\scriptstyle \pi _ { i } = [ \mathrm { G E } ( \mathrm { p i d } , \ 1 0 1 )$ , LE(pid, 110), LE(price, 10)] and p<sub>j</sub>=[GE(pid, 101), LT(pid, 999), GT(pname, MIN<sup>\_</sup>RANGE), GT(price, MIN<sup>\_</sup>RANGE), LT(price, MAX<sup>\_</sup>RANGE), OR (GT (price, 5), LT(unit<sup>\_</sup>cost, MAX<sup>\_</sup>RANGE)]. It can be seen that the filter list $\pi _ { j }$ has two extra dimensions relative to $\pi _ { i } ,$ namely, unit<sup>\_</sup>cost and pname. The projection of $\pi _ { j }$ over $\pi _ { i }$ reduces the dimensions of p<sub>j</sub> by getting rid of clauses LT(unit<sup>\_</sup>cost, MAX<sup>\_</sup>RANGE) and GT(pname, MIN<sup>\_</sup>RANGE). The elimi-nation of these clauses is possible because they are dummy clauses that involve MIN<sup>\_</sup>RANGE or MAX<sup>\_</sup> RANGE. Note that LT(unit<sup>\_</sup>cost, MAX<sup>\_</sup>RANGE) implicitly implies that unit<sup>\_</sup>cost is not bounded since MAX<sup>\_</sup>RANGE represents one extreme boundary of the domain of unit<sup>\_</sup>cost. The projected filter list is given by $\pi _ { j i } = [ \mathrm { G E } ( \mathrm { p i d } ,$ , 101), LT(pid, 999), GT(price,

![](/api/attachments/99M75GUN/fulltext/images/5c66d5e3993863d9c3a9309cc9cbf33168249b06628f94a5b90a797df4a2e9dc.jpg)  
Fig. 3. Filter space (FS) indicated by the shaded region is defined by $\scriptstyle \pi = [ { \mathrm { G E } } ( { \mathrm { p i d } } , 1 0 1 )$ , LE(pid, 103), GE(cost,10.0), LE(cost, 20.0)].

5), LT(price, MAX<sup>\_</sup>RANGE)] and has the same dimensions as the filter list $\pi _ { i } .$ . The filter list of $\pi _ { i }$ is covered in $\pi _ { j i }$ (i.e., there is a match) provided the following two conditions are satisfied. First, the dimensions of $\pi _ { i }$ are the same as the dimensions of $\pi _ { j i }$ (which is true in the current example since both contain pid and price). Second, the filter space of $\pi _ { i }$ is covered (i.e., subspace) in the filter space of $\pi _ { j i } .$ Readers can easily verify that this is also true. Hence, the logical conditions associated with the filter list $\pi _ { i }$ in the current example match with the logical conditions in the filter list $\pi _ { j } .$

## 3. Data scope hierarchies and data sources

A data scope hierarchy such as in Fig. 2 represents an organizational view of the data. In this section, we first present definitions and results related to a data scope hierarchy. We then describe how data sources in a data scope hierarchy can provide data for a query.

Definition 1. Let $I _ { j }$ denote an index set at level $j \geq 1$ The ith data scope hierarchy associated with $I _ { j }$ at level j, $i { \in } I _ { j } ,$ and given by $T _ { i j } ,$ is a finite set of data scopes such that: (i) given a set of m scopes where $m { > } n \geq 0 .$ there is a specially designated scope called the root scope $r _ { i j }$ at level $j ;$ (ii) the remaining $m - 1$ data scopes are partitioned into $n \geq 0$ disjoint sets $T _ { k , j + 1 } ,$ $T _ { k + 1 , j + 1 } , . . . , T _ { k + n - 1 } , j + 1$ , k>0, where each of these sets is a data scope hierarchy at level $j + 1$ with root scopes $r _ { k , j + 1 } , \ r _ { k + 1 , j + 1 } , . . . , r _ { k + n - 1 , j + 1 }$ at level $j + 1$ (iii) $r _ { i j } = r _ { k , j + 1 } \cup r _ { k + 1 , j + 1 } \cup . . . \cup r _ { k + n - 1 , j + 1 } . \ T _ { k , j + 1 , * } . . . ,$ $T _ { k + n - 1 , j + 1 }$ are known as child hierarchies of $T _ { i j } .$

The example in Fig. 2 represents a data scope hierarchy at three levels. The index set $I _ { 2 }$ at level 2 is given by $I _ { 2 } { = } \{ 1$ (for US), 2 (for Canada)}. For the first scope hierarchy at level 2, the root scope is US and the remaining scopes are partitioned into three child hierarchies {VA}, {CA} and {FL}. Note that $\mathrm { U S } { = } \{ \mathrm { V A } \} \cup \{ \mathrm { C A } \} \cup \{ \mathrm { F L } \}$ (from (iii) in Definition 1).

Definition 2. The root data scope hierarchy is the ith data scope hierarchy at level j = 1, when $i = 1 , \ \vert I _ { j } \vert = 1$

According to Definition 2, the data scope hierarchy with the root scope at level 1 is the root data scope hierarchy. The example in Fig. 2 is a root data scope hierarchy, whereas the hierarchy with the root scope US is not a root data scope hierarchy. Scope hierarchies facilitate the resolution of query scopes.

To facilitate model composition, a control procedure operates on the metadata representing models and

k  m = h‘vaprod’,

(price, 10). The clause LE(price, 10) is non-binding with respect to the tighter clause LE(price, 5) and should not be present in the filter list if the tighter clause LE(price, 5) is present. The following example illustrates the keyvalue set definition of the data in VAPROD (Table 1).

{pid},

{pname, price, quantity, unit<sup>\_</sup>cost},

[GE(pid, 101), LT(pid, 103), GE(pname, MIN<sup>\_</sup>RANGE), LT(pname, MAX<sup>\_</sup>RANGE), GE(price, 0.0), LT(price, MAX<sup>\_</sup>RANGE), GE(quantity, 0), LT(quantity, MAX<sup>\_</sup>RANGE), GE(unit<sup>\_</sup>cost, 0.0), LT(unit<sup>\_</sup>cost, MAX<sup>\_</sup>RANGE)]

databases available to a DSS user. A metadata construct that is used for representing relational data associated with database tables as well as those associated with model inputs and outputs is the key-value set definition. Data sources in a DSS such as database tables and model outputs can be represented by key-value set definitions. Definition 3 extends the definition of key-value set definition in Appendix A to incorporate data scopes.

Definition 3. A key-value set definition $( \kappa - \nu )$ of a relation in the context of a root data scope hierarchy $T _ { 1 1 }$ consists of five parts: $\kappa - \nu = \left. \eta , \kappa , \nu , \iota , \pi \right.$ , where g is an identifier string of the key-value set definition, $\kappa { = } \{ k _ { 1 } { , } . ~ . ~ . , k _ { k } \} , ~ k _ { 1 } { , } . ~ . ~ . , k _ { k }$ are strings that represent the names of attributes participating in the primary key of the relation, $\nu { = } \{ \nu _ { 1 } { , } . ~ . ~ . , \nu _ { m } \} , ~ \nu _ { 1 } { , } . ~ . ~ . , \nu _ { m }$ are strings that represent the names of the non-key (i.e., value) attributes in the relation $( \nu _ { 1 } , . . . . , \nu _ { m } @ \kappa )$ , i is the data scope attribute of the relational data such that $\imath { = } \{ r _ { i j }$ or $j \}$ where $r _ { i j }$ is the root scope of the ith data scope hierarchy $T _ { i j }$ at level j of $T _ { 1 1 }$ , and p is the filter list containing only binding clauses that defines the filter space of the relation.

As per Definition 3, a key value set definition describes a relation. The key and value sets describe the key and the non-key attributes of the relation respectively. The data scope attribute locates the key value set definition in a data scope hierarchy. The filter list contains only binding clauses that specify the range of values associated with the various key and value attributes. Consider two clauses LE(price, 5) and LE

The filter list of VAPROD contains clauses such that variables corresponding to all attributes (both key and non-key) are present, i.e., the filter list has the maximum dimensional property. To enable the filter list to have the maximum dimensional property, so that the corresponding filter space has all the dimensions of the relational table, dummy clauses of the form /(Variable, MAX<sup>\_</sup>RANGE) or /(Variable, MIN<sup>\_</sup>RANGE) for variables not present in the filter list are added to the filter list without violating the logical conditions in the filter list.

In the above example, the data scope attribute value VA associates the key-value set definition with the scope VA identified in the data scope hierarchy (Fig. 2). Data scope attributes are useful in many ways. First, a data scope attribute can be used to distinguish tuples belonging to two different tables with the same key values (for instance, tuples with pid 101 and 102 present in Tables 1 and 2), while matching a query to data sources. Second, data scope attribute values can facilitate automating the integration of data in response to queries. For example, if a user poses a query that requires the total quantity of product (with pid = 101) available from plants in US, the data scope attribute of the query can be simply set to US. Based on the hierarchy presented in Fig. 2, a query processing procedure can do the necessary selection of tuples from tables VAPROD, CAPROD and FLPROD followed by aggregation of the quantity field based on a usersupplied aggregation operator. Thus, the data scope attribute can convey a high level of semantic information that can shield users from specifying lower level details. In our implementation, the user has to specify the aggregation operator to use (sum, avg, etc.) in response to the query by the system.

Key-value set definitions and hence the attributes in the key-value set definitions are associated with various levels in the data scope hierarchy (see Definition 3). The value of an attribute at a higher level in the hierarchy can be computed using values of attributes at a lower level in the data scope hierarchy according to the following proposition.

Proposition 1. Let $( a _ { k } ) _ { i j }$ denote the data vector for the numeric attribute $c { \in } { \nu }$ of a key-value set definition $\kappa - \nu = \left. \eta , \kappa , \nu , r _ { i j } , \pi \right.$ associated with the root scope $r _ { i j }$ in the data scope hierarchy $T _ { i j } , O P { = }$ {sum (total), avg (average), max (maximum), min (minimum)} and $\{ ( a _ { k } ) _ { g , j + l } \} _ { g = m , . . . , s } ^ { 2 }$ denote the set of data vectors for c when c<sup>a</sup> $\nu _ { g } o f \kappa - \nu _ { g } = \langle \eta _ { g } , \kappa _ { g } , \nu _ { g } , r _ { g , j } + \jmath , \pi _ { g } \rangle , g = m , . . . , s ,$ where $\kappa - \nu _ { g }$ is a key-value set definition at level $j + I$ that is associated with the root scope of a child hierarchy of $T _ { i j } ,$ , and the range $m , . . . , s$ represents indexes of all child hierarchies of $T _ { i j }$ at level $j + l .$ . If $\kappa = \kappa _ { g }$ and $\pi = \pi _ { g } , g = m , . . . , s $ , then $( a _ { k } ) _ { i j } = o p _ { g = m , . . . , s }$ $( ( a _ { k } ) _ { g , j + } , )$ where op<sup>a</sup>OP.

Proof. Let $\pmb { R } _ { c }$ and $R _ { g c }$ denote the relations associated with $\kappa - \nu _ { c } = \langle \eta , \kappa , \{ c \} , r _ { i j } , \pi \rangle$ and $\kappa - \nu _ { g c } = \langle \eta _ { g } , \kappa _ { g } , \{ c \}$ ， $r _ { g , j + 1 } , \pi _ { g } \rangle$ , respectively, where $g = m , . . . , s .$ Since $r _ { i j } =$ $\cup _ { g = m , . . . , s } r _ { g , j + 1 } { \bf b } { \bf y }$ definition (Definition 1), $R _ { c }$ can be obtained by merging all $R _ { g c } , \ g = m , . \ . , s .$ . Furthermore, since $\kappa = \kappa _ { g }$ for $g = m , . . . , s ,$ , the total number of tuples in $R _ { c }$ is required to be equal to the total number of unique key values in $\cup _ { g = m , . . . , s } R _ { g c } .$ Since multiple tuples could exist in $R _ { c } = \cup _ { g = 1 , . . . , n } R _ { g c }$ with the same key values, this would violate the relational principle that requires a single tuple for every key value associated with a relation. Hence, multiple tuples for a given key value can be combined using an aggregation operator $\mathbf { \boldsymbol { \mathrm { o p } } } { \in } \mathbf { \boldsymbol { \mathrm { O P } } }$ as follows: $( a _ { k } ) _ { i j } =$ $\mathrm { O P } _ { g = m , . . . , s } ( ( a _ { k } ) _ { g , j + 1 } )$ . This results in a singleton value for every key value. 5

To illustrate Proposition 1, consider the example query that requires the quantity (i.e., the inventory count) of all products in US. The scope of this query is $\mathrm { U S } { = } \{ \mathrm { V A } \} \cup \{ \mathrm { C A } \} \cup \{ \mathrm { F L } \}$ . Therefore, the relations of the key-value sets associated with the data scopes $\mathrm { V A } .$ CA and FL (i.e., VAPROD, CAPROD and FLPROD)

are combined to obtain a table with six rows with key values pid = 101, 102, 103, 104, 105, and 106. The quantity vector returned for the query is [200, 300, 50, 50, 50, 50] (see Tables 1 –3). The aggregation operator used is the sum operator and is specified by the user.

Definition 4. Let a query be given by the key-value set definition $\boldsymbol { \kappa } - \boldsymbol { \nu } ^ { \mathrm { q } } = \langle \eta ^ { \mathrm { q } } , \kappa ^ { \mathrm { q } } , \nu ^ { \mathrm { q } } , \imath ^ { \mathrm { q } } , \pi ^ { \mathrm { q } } \rangle$ and a data source be given by $\kappa - \nu ^ { \mathrm { s } } = \langle \eta ^ { \mathrm { s } } , \kappa ^ { \mathrm { s } } , \nu ^ { \mathrm { s } } , \imath ^ { \mathrm { s } } , \pi ^ { \mathrm { s } } \rangle$ . The data scope attribute $\imath ^ { \mathrm { q } }$ of $\kappa - \nu ^ { \mathrm { q } }$ matches with the data scope attribute $\imath ^ { \mathrm { s } }$ of $\kappa - \nu ^ { \mathrm { s } }$ in the context of a given scope hierarchy $T _ { i j } ,$ , when $\boldsymbol { l } ^ { \mathrm { q } } = \boldsymbol { l } ^ { \mathrm { s } }$ or $\imath ^ { \mathrm { q } } =$ level of $\imath ^ { \mathrm { { s } } }$ in $T _ { i j }$

The data scope attribute of a query can contain a specific data scope such as FL or a generic scope value such as State that represents the level in the scope hierarchy. In the latter case, the data scope attribute of the query can match with the data scope attribute of any key value set definition of a data source whose scope attribute is at the level of State. We now specify the conditions under which there is a match between a query and a data source using Proposition 2, which extends a proposition in Ref. [9] to include scope matching.

Proposition 2. A single data source $\kappa - \nu ^ { s } = \langle \eta ^ { s } , \kappa ^ { s }$ $\nu ^ { s } , \iota ^ { s } , \pi ^ { s } \rangle$ can provide data for a query defined by the key-value set definition $\kappa - \nu ^ { q } = \langle \eta ^ { q } , \kappa ^ { q } , \nu ^ { q } , \imath ^ { q } , \pi ^ { q } \rangle$ when the following conditions are true:

0.1. $\kappa ^ { \mathrm { q } } = \kappa ^ { \mathrm { s } }$

0.2. $ { \boldsymbol \nu } ^ { \mathrm { q } } \subseteq  { \boldsymbol \nu } ^ { \mathrm { s } }$

0.3. $\boldsymbol { l } _ { \mathbf { q } }$ matches with $l _ { \mathrm { s } }$

0.4. The filter list $\pi ^ { \mathrm { q } }$ of $\kappa - \nu ^ { \mathrm { q } }$ is covered in the filter list $\pi ^ { \mathrm { s } }$ of $\kappa - \nu ^ { \mathrm { s } }$ (Definition VIII in Appendix A)

Proof. Since all the data definitions in $\kappa ^ { \mathrm { q } }$ match with the data definitions in $\kappa ^ { \mathrm { s } } .$ , every tuple identified by the query can map to a unique tuple of the data source. Furthermore, since all the data definitions in $\nu ^ { \mathrm { q } }$ match with the data definitions contained in $ { \boldsymbol \nu } _ { \mathrm { ~ ; ~ } } ^ { \mathrm { s } }$ , therefore, the data attributes required by the query are contained in the data source. Now since the scope attribute of the query matches with the scope attribute of the data source, the data source is at the same level in the data hierarchy as that of the data needed for the query. Furthermore, since $\pi ^ { \mathrm { q } }$ is covered in $\pi ^ { \mathrm { s } }$ , the filter space of the query is contained in the filter space of the source after necessary projection to equalize dimensions. Therefore, the tuples required by the query are contained in the data source. 5

There could be situations where a single data source may not be able to provide all the data needed for a query. Proposition 3, which an extension of a proposition in Ref. [9] holds when multiple data sources are needed to provide data for a query.

Proposition 3. A minimal set of data sources $S { = } / \kappa -$ $\nu _ { j } ^ { s } \} _ { j } = I , \ldots , m ,$ where $\kappa - \nu _ { j } ^ { s } = \langle \eta _ { j } ^ { s } , \kappa _ { j } ^ { s } , \nu _ { j } ^ { s } , \iota _ { j } ^ { s } , \pi _ { j } ^ { s } \rangle$ , can provide data for a query defined by key-value set $\kappa - \nu ^ { q } = \left. \eta ^ { q } , \kappa ^ { q } , \nu ^ { q } , \imath ^ { q } , \pi ^ { q } \right.$ when the following conditions are true:

0.1. $\kappa ^ { \mathrm { q } } = \kappa _ { j } ^ { \mathrm { s } } , j = 1 , \ldots , m$

0.2. $\boldsymbol { \nu } ^ { \mathrm { q } } \subseteq \boldsymbol { \nu } _ { j } ^ { \bar { \mathrm { s } } } , \mathrm { j } = 1 , \ldots , m$

$$
\iota^ {\mathrm{q}} = \bigcup_ {j = 1, \dots , m} \iota_ {j} ^ {\mathrm{s}} \text {   or   } \iota^ {\mathrm{q}} = \text { level   of   } \iota_ {j} ^ {\mathrm{s}},
$$

$$
j = 1, \dots , m,
$$

0.4. $X _ { \pi { \bf q } = } X _ { \pi j { \bf q } } , j = 1 , . . . , m ,$ , where $X _ { \mathrm { q } }$ and $X _ { \pi j \mathrm { q } }$ are the set of unique variables appear-ing in $\pi ^ { \mathrm { q } }$ and $\pi _ { j \mathrm { q } } ^ { \mathrm { s } } ,$ respectively, $\pi _ { j \mathrm { q } } ^ { \mathrm { s } }$ is obtained by projecting $\pi _ { j } ^ { \mathrm { s } }$ over $\pi ^ { \mathrm { q } }$ (De-finition VII)

0.5. $\mathrm { F S } _ { \pi \mathbf { q } } \subseteq \cup _ { j = 1 , . . . , m } \mathrm { F S } _ { \pi j \mathbf { q } }$ where $\mathrm { F S } _ { \pi \mathrm { q } }$ and $\mathrm { F S } _ { \pi j \mathrm { q } }$ denote the filter spaces associated with $\pi ^ { \mathrm { q } }$ and $\pi _ { j \mathrm { q } } ^ { \mathrm { s } } ,$ respectively, and

0.6. No subset of S satisfies (5).

Proof. Since all data definitions in $\kappa ^ { \mathrm { q } }$ match with the data definitions in $\kappa _ { j } ^ { \mathrm { s } } , j { = } 1 , \ldots , m ,$ , every tuple identified by the query can map to a unique tuple of a data source. Furthermore, since all the data definitions in $\nu ^ { \mathrm { q } }$ match with data definitions contained in $\nu _ { j } ^ { \mathrm { s } } ,$ $j = 1 , . . . , m ,$ , the data attributes required by the query are therefore contained in the data source. If $\boldsymbol { l } ^ { \mathrm { q } } = \bigcup _ { j = 1 , . . . , m } \boldsymbol { l } _ { j } ^ { \mathrm { s } }$ then $\boldsymbol { l } _ { j } ^ { \mathrm { s } }$ is a child scope (Definition 1) and the union of scopes of the data sources is equivalent to the scope of the query. When $\scriptstyle { \boldsymbol { \imath } } ^ { \mathrm { q } } = \log \mathrm { e l }$ of $\begin{array} { r } { \imath _ { j } ^ { \mathrm { s } } , } \end{array}$ $j = 1 , . . . , m$ , then the data scope attributes match according to Definition 4. In this case, the scope of the query is at the same level as that of the data sources. The filter spaces of different data sources after necessary projection have the same dimensions as the filter space of the query since $X _ { \pi \mathrm { q } } = X _ { \pi j \mathrm { q } } , \ j = 1 , . . . , m$ $\mathrm { F S } _ { \pi { \bf q } } \subseteq \cup _ { j = 1 , . . . , m } \mathrm { F S } _ { \pi j { \bf q } } ,$ implies that all the tuples required by the query are contained in the set of data sources S. According to (6), S is a minimal set, since no subset of S provides all the data needed for the query. 5

## 4. Model composition

Model composition is facilitated by a control procedure that creates a composite model using metadata information of existing models and databases. A key-value set definition, which represents the metadata of relations can be embedded in metadata structures of database tables and models.

```txt
DB = ⟨⟨‘vaprod’,
{pid},
{pname, price, quantity, unit_cost},
VA,
[GE(pid, 101), LT(pid, 103), GE(pname, MIN_RANGE),
LT(pname, MAX_RANGE), GE(price, 0.0), LT(price, MAX_RANGE),
GE(quantity, 0), LT(quantity, MAX_RANGE),
GE(unit_cost, 0.0), LT(unit_cost, MAX_RANGE)]
⟩,
```

The metadata of a database table, which is represented by a database template, is an ordered triple $\mathrm { D B } =  \kappa - \nu _ { ; }$ , Source, Copyi where $\kappa - \nu$ represents the key-value set definition that describes the data stored in the database table, Source is a string that encodes the physical location and access method of the database, and Copy is a string that designates the database as primary or secondary. The primary copy is the database copy closest to the DSS user. The Source string can be represented by the following format: AccessMethod::DatabaseName/Path::TableName. An example of a database template representing the metadata of the table VAPROD (Table 1) follows next.

odbc::acad::prodtable,

primary

In the above example, the database template represents the relational table prodtable that is associated with an ODBC data source acad in the resource the filter list of all model inputs as well as the clauses from Filter. An example of a model source template is as follows.

M = h‘demand<sup>\_</sup>forecast’,

{h‘price’, {pid}, {price}, US, [GE(pid, 101), LT(pid, 105), GT(price, Min<sup>\_</sup>Range),

LT(price, Max\_Range)]i},

{h‘demand’, {pid},{demand}, US, {GE(pid, 101), LT(pid, 105), GT(price, Min\_Range),

LT(price, Max\_Range), GT(demand, Min\_Range), LT(demand, Max\_Range)}i}, [assumption(‘linear demand function’), assumption(‘valid for 2001 forecast in the US market’), assumption(‘valid for products 101– 104’)],

‘corba::134.126.40.232::d

database. This table location contains the primary copy of VAPROD data for a given user. In a distributed setting where files are replicated, if the primary copy is not accessible to a user, a secondary copy is accessed. The filter clauses specify the range of attribute values available at the data source. It can be observed that except for pid, all other attributes have values in the range: Min<sup>\_</sup>Range to Max<sup>\_</sup>Range.

A model template represents the metadata of models in a DSS and is composed of six parts, M = hModelName, Input, Output, Assumption, Filter, Sourcei, where ModelName identifies the model, Input is a set of key-value set definitions representing model input relational data, Output is the set of keyvalue set definitions representing model output relational data, Assumption is a set of clauses or strings representing model assumptions that are not related to any single model input, Filter is the set of filter clauses representing constraints among model inputs and can only contain clauses of Type 1a and clauses of Type 2 that are in turn composed of clauses of Type1a and Source is a string that encodes the physical location and access method of the model executable. A formal definition of a model source template can be found in Appendix A (Definition VI). The suggested format for Source is AccessMethod::HostName/Address/Path:: ModelExecutable/ Server. The filter list of a model output includes

In the above example, the model template maps the demand<sup>\_</sup>forecast model to a model executable on a host whose IP address [10] is 134.126.40.232 and the access method used is CORBA-based object request brokers [28] (an example of Source string representing CGI scripts executed via HTTP is http:\\ estes.jmu.edu:: /cgi-bin/financial.pl). The demand<sup>\_</sup> forecast model has only one input i.e., price and one output i.e., demand. Both input and output are represented by two separate sets of key-value set definitions. The input price is a relational table of one or more tuples with two attributes pid and price. The output demand is a relational table of one or more tuples with two attributes pid and demand. The filter clauses indicate that the model can only be used to compute demands for products in the range pid = 101. . .104. The underlying assumptions associated with the model include: (1) the use of linear demand function, (2) validity for 2001 forecast, and (3) validity for products 101–104. As seen above, the filter term of the model template, i.e., Filter is an empty list. Filter can be non-empty list only when constraints among model inputs have to be enforced. Note that the filter list of the model output demand contains the filter clauses of the model input price. This follows from the requirement that the filter list of all model outputs include the union of filter lists of all model inputs, thereby enabling model preconditions to be related to model inputs. In order to maintain the clarity of presentation in this paper, we assume that attributes and variables are named such that there are no homonyms and synonyms. The restriction on synonyms is relaxed in our implementation. A query processing procedure operates on database and model templates to create and execute composite models.

A high level description of the query processing procedure used for generating and executing composite models is presented below. This procedure is an extension of the query processing procedure described in Ref. [9] and includes support for data scopes, and data distribution. This procedure receives a query in the form of a key value set definition j  m. The query processing procedure then recursively processes j  m as follows.

Step 1. Search for a database template designated as primary, such that j  m matches the key-value set of the database table (according to Proposition 2). If successful, retrieve data and place it in Out. Search is successful. Stop. Step 2. Same as Step 1, except that the database table is secondary. Step 3. Search for a model template such that, j  m matches the key-value set of one of the outputs of the model (according to Proposition 2) and the model assumptions are valid. If successful, update the filter list of the key value set of model inputs and the list of inter-input constraints (i.e., Filter) by incorporating filter clauses of the query (Definition X in Appendix A). Let the set $( \mathrm { K V _ { i n p u t } } )$ contain key-value definitions associated with all model inputs (i.e., new subordinate queries). $\mathrm { S e t ~ O U T _ { \mathrm { i n p u t } } } { } ^ { \bullet } = \bigotimes , \mathrm { K V ^ { u } } = \bigotimes .$ <sub>3.1. If KV</sub><sup>u</sup> p F, then go to 4.

Else if $\mathrm { K V _ { i n p u t } } = \emptyset$ , filter model input data stored in ${ \mathrm { O U T } } _ { \mathrm { i n p u t } }$ based on clauses specified in Filter of the model, execute model, put model output in OUT. Search is successful. Stop;

Else remove a key-value set j  m from ${ \mathrm { K V } } _ { \mathrm { i n p u t } } .$ 3.2. Execute Step 1, with the change that on success go to Step 3.1. 3.3. Execute Step 2, with the change that on success go to Step 3.1. 3.4. Execute Step 3, with the change that on success go to Step 3.1. 3.5. Execute Step 4, with the change that on success go to Step 3.1. 3.6. Execute Step 5, with the change that on success go to Step 3.1. Step 4. Get data from multiple database tables and/or model executions all at the same level $\gamma$ in the data scope hierarchy. If successful, place data in Out. Stop. Step 5. Repeat Steps 1–4 for every item at the child scope of the current scope $\gamma$ in the data scope hierarchy. If successful, combine the tuples obtained for all children scopes (as per user specified aggregation operators, see Proposition 1) and place it in Out. Stop. Step 6. Search is unsuccessful. Stop.

Detailed description of Step 4 is presented in Ref. [9]. In this paper, we illustrate the query processing procedure using two examples. First, consider the query that requires the sales forecast for products with pid = 101 and pid = 102 in the US market. In this case, the query can be represented by the following set of key-value set definition kv = h‘query1’, {pid}, {demand}, US, [GE(pid, 101), LE(pid, 102)}. Since none of the database tables contain demand as an attribute therefore, Steps 1 and 2 will not succeed. The metadata of a demand-forecasting model exists as follows:

[assumption(‘linear demand function’), assumption(‘valid for 2001 forecast in the US market’), assumption(‘valid for products 101 –104’)],

‘corba::134.126.40.232::d’

This model contains demand as an output attribute. The key attribute of the query, i.e., pid matches with the key attribute of ‘demand’, the query attribute demand matches with the model output demand. The filter list of the query [GE(pid, 101), LE(pid, 102)] is covered in the filter list of the model output, i.e., [GE(pid, 101), LT(pid, 105), GT(price, Min<sup>\_</sup> Range), LT(price, Max<sup>\_</sup>Range), GT(demand, Min<sup>\_</sup> Range), LT(demand, Max<sup>\_</sup>Range)]. Furthermore, the user affirms all the assumptions of the model. Thus, model M can provide data for the query. As per Step 3, the filter list of the query is incorporated in the filter list of the model input by simply adding GE(pid, 101), and LE(pid, 102) to [GE(pid, 101), LT(pid, 105), GT(price, Min<sup>\_</sup>Range), LT(price, Max<sup>\_</sup>Range)] and then by removing the non-binding clause LT(pid, 105) and dummy clauses involving variables not present in the query: GT(price, Min<sup>\_</sup> Range) and LT(price, Max<sup>\_</sup>Range). The updated filter list of the input as follows: [GE(pid, 101), LE(pid, 102)]. The updated key-value set definition of the model input ‘price’, kv = h‘price’, {pid}, {price}, US, [GE(pid, 101), LE(pid, 102)]i is now posed as a new query.

While processing the new query, Steps 3.2 to 3.5 do not succeed since the scope of prices in database tables (i.e., VAPROD, and CAPROD) are at the level of states whereas the query’s scope is US. Step 3.6 is then executed. This step is applied to every child of US that includes the scopes in the set {VA, CA, FL}. Appropriate tuples from the lower level tables VAP-ROD (Table 1), and CAPROD (Table 2) are obtained. FLPROD (Table 3) fails to provide any tuples since the filter list of the query is not covered in the filter list of FLPROD. The user then specifies the avg (average) operator as the aggregation operator to combine prices of products obtained from VAPROD and CAPROD. Now that model input data acquisition is completed, the model is executed in Step 3.1 and the appropriate result for the original query is then returned.

Now consider the query: ‘‘Get total quantities of products 101–106 available at US plants’’. This query can be represented by the following key-value set definition: kv = h‘query2’, {pid}, {quantity}, US, [GE(pid, 101), LE(pid, 106)]i. Note that the scope of the query is US whereas data is stored at the level of states (for example, the scope of the database table VAPROD is VA). Thus, Steps 1–2 will not succeed. Step 3 will also not succeed since there are no models that can generate quantity of products in the inventory. Step 4 will also not succeed since there are no database tables or models that generate quantity at the scope level of US. Step 5 is then applied to every child of US that includes the scopes in the set {VA, CA, FL}. Data from the lower level tables VAPROD (Table 1), CAPROD (Table 2), and FLPROD (Table 3) are thus obtained and combined. In this case, data from the quantity field is combined using the sum operator on a ‘product by product’ basis. The sum operator is an additional input from the user. The quantity value computed for product with pid = 101 is 200.

## 5. Systems architecture

A systems architecture is an important part of our solution to support model composition and execution in a distributed environment. The architecture presented below in Fig. 4 has the following features: (1) enables model/data distribution and implementation to be transparent to the user, (2) supports a high degree of automation during model composition and execution, and (3) permits a variety of databases, model implementations, protocols and access methods to be used in order to leverage existing organizational resources.

A user interacts via the user interface and submits queries and additional information as needed. A query engine (actually an inference engine), by applying rules in the Rule Base, selects data and model sources (represented by database and model source templates in the resource database) for answering queries. The query engine interleaves model composition with model execution by passing control to the model interface module when a component model is selected for execution, and to the data interface module when data from the selected database is to be retrieved. Every model has a driver that is specific to the model. The model driver has the logic to translate model inputs that are presented to it in a standard data exchange format into a model specific format, and to translate model outputs back to a standard data exchange format. The model driver logic is dependent on the access method and the middleware used to execute the model. For example, if a model is executed remotely via an object request broker, then various stub routine calls are made in the model driver. The data interface module generates an SQL query based on the user request and then passes the SQL string to an appropriate access method module such as an ODBC module. The access method module interfaces with an appropriate data protocol for communicating the query to a remote database site. The data retrieved is then translated into a standard data exchange format by the data interface module.

![](/api/attachments/99M75GUN/fulltext/images/8ae1dce4b278c474c69de2a77cfd465dcac64488bce8b1e0ae0095fd26827122.jpg)  
Fig. 4. Systems architecture.

A prototype has been implemented to validate the above architecture. This prototype uses a variety of access methods such as HTTP, CORBA, and ODBC for executing models and for retrieving data from databases. Using this prototype, model composition and execution involving three component models and data from two databases: Oracle and Microsoft Access, located on multiple machines, has been successfully accomplished. Implementation details can be found in Ref. [8].

## 6. Conclusion

In response to the problem of leveraging existing data and model resources that are distributed at various sites within an organization for decision making, we have addressed the problem of model composition in a distributed environment. We have extended the filter space approach [9] to include data scope hierarchies in order to facilitate model composition when there are overlapping key-values in a replicated environment and when data scopes vary. Our approach can be applied to a variety of generalization/specialization hierarchies, when higher-level data is obtained by combining lower level data. This is especially useful when data is stored at various levels such as in data warehouses, transactional databases, etc., and data from these sources need to be integrated to facilitate model composition.

As part of the solution to the research problem in this paper, we have also presented a systems architecture that facilitates a high degree of automation during model composition and execution, as well as enables model/data distribution and implementation to be transparent to the user. We have validated key ideas of this paper using an implementation. An important research problem that we are interested in investigating in the future is to test the scalability of our approach by deploying largescale production systems in actual data warehousing environments.

## Acknowledgements

The author would like to thank the anonymous referees for their valued comments.

## Appendix A. Filter space approach definitions

All definitions in this section have been taken from Ref. [9]. The following notations are used in the definitions.

<table><tr><td>V</td><td>represents a set of variables</td></tr><tr><td>C</td><td>represents a set of constants</td></tr><tr><td>Φ</td><td>represents a predicate</td></tr></table>

Definition I. A filter clause is defined as follows.. Type 1: A predicate of two places, $\varPhi ( \alpha _ { 1 } , \alpha _ { 2 } )$ is a filter clause when $\scriptstyle \phi \in \{ \mathrm { L T } ( < ) , \mathrm { L E } ( < = ) , \mathrm { G T } ( > ) , \mathrm { G E } ( > = ) \}$ ， and $\alpha _ { 1 } , \alpha _ { 2 }$ are non-predicate terms as follows: (a) $\alpha _ { 1 } , \alpha _ { 2 } { \in } V$ represent variables; (b) $\alpha _ { 1 } \in V ,$ and $\alpha _ { 2 } { \in } C$ is a constant such that MIN<sup>\_</sup>RANGE V a<sub>2</sub> V MAX<sup>\_</sup> RANGE, where MIN<sup>\_</sup>RANGE and MAX<sup>\_</sup>RANGE are low and high values, respectively, that are never taken by $\alpha _ { 1 } .$ . Type 2. A predicate of n places, $\varPhi ( \alpha _ { 1 } , \ldots . . , \alpha _ { n } )$ is a filter clause when $\varPhi = \mathrm { O R }$ (disjunction), $\mathcal { \alpha } _ { 1 } , . . . , \mathcal { \alpha } _ { n }$ are predicates of Type 1 and $n { > } 0$

Definition II. A filter list is a set of filter clauses in the conjunctive normal form [24].

Definition III. Let the filter list $\pi { = } \{ \Phi _ { k } ( X _ { \pi k } ) \} _ { k { = } 1 , . . . , m } ,$ where $X _ { \pi k }$ is the set of variables that participate in the filter clause $\scriptstyle \phi _ { k } ( \cdot ) , \ X _ { \pi } = \cup _ { k = 1 , . . . , m } X _ { \pi k } = \{ x _ { 1 , . . . , x _ { n } } \} \subseteq V$ is the set of unique variables that participate in p, and the total dimensions $n = \left| X _ { \pi } \right|$ . The filter space of p is given by ${ \mathrm { F S } } _ { \pi } { = } \{ c | c \in C ^ { n } \vee$ substitution $\theta { = } \{ c _ { 1 } / x _ { 1 } , . . . . , c _ { n } /$ $x _ { n } \} \lor ( \lor _ { k = 1 , . . . , m } \Phi _ { k } ( X _ { \pi k } ) \theta ) \}$

Definition IV. A key-value set definition $( \kappa - \nu )$ of a relation consists of four parts: $\kappa - \nu = \langle \eta , \kappa , \nu , \pi \rangle$ , where g is an identifier string of the key-value set definition, $\kappa { = } \{ k _ { 1 } , . . . , k _ { k } \} , k _ { 1 } , . . . , k _ { k } { \in } C$ are strings that represent the names of attributes participating in the primary key of the relation, $\nu = \{ \nu _ { 1 } , . . . , \nu _ { m } \} , ~ \nu _ { 1 } , . . . , \nu _ { m } { \in } C$ are strings that represent the names of the non-key (i.e., value) attributes in the relation $( \nu _ { 1 } , . . . . , \nu _ { m } @ \kappa )$ and p is the filter list containing only binding clauses that defines the filter space of the relation.

Definition V. A database template is an ordered pair $\mathrm { D B } = \left. \kappa - \nu _ { \mathrm { - } } \right.$ , Databasei, where $\kappa - \nu = \langle \eta , \kappa , \nu , \pi \rangle$ is the key-value set definition of the database with filter list p having the maximum dimensional property and corresponding filter space $\mathrm { F S } _ { \pi } \neq \emptyset$ ; Database is a string that encodes the database name, path and access method.

Definition VI. A model template M is composed of six parts, M = hModelName, Input, Output, Assumption, Filter, Sourcei, where ModelName is a string that identifies the model; Input $: = \{ \kappa - \nu _ { j } ^ { i } \} _ { j = 1 , . . . , p }$ is the set of key-value set definitions $\kappa - \bar { \nu } _ { j } ^ { i } { = } \langle \eta _ { j } ^ { i } { , } \kappa _ { j } ^ { i } { , } \nu _ { j } ^ { i } { , } \pi _ { j } ^ { i } \rangle$ representing p model input relational data, where

(1) $\pi _ { j } ^ { i } , \ j = 1 , . . . , p .$ , has the maximum dimensional property,

(2) $\lnot \ = \ \ l ( \exists \phi _ { 1 } ( x ) { \in } { \pi } _ { j } ^ { i } \exists \phi _ { 2 } ( x ) { \in } { \pi } _ { k j \ne \ k } ^ { i } ( \phi _ { 1 } ( x ) { \ne } \ \phi _ { 2 } ( x ) ) )$ where $\varPhi _ { 1 } ( x )$ and $\varPhi _ { 2 } ( x )$ are clauses of Type 1b that have the same predicate symbols and variable x,

(3) the filter space associated with $\pi _ { j } ^ { i } , \ \mathrm { F S } _ { \pi j } ^ { i } \neq \ \varnothing$ $j = 1 , \hdots . \hdots p ;$

Output $= \{ \kappa - \nu _ { k } ^ { o } \} _ { k = 1 , . . . , q }$ is the set of key-value set de-finitions $\kappa - \nu _ { k } ^ { o } = \langle \bar { \eta _ { k } ^ { o } } , \kappa _ { k } ^ { o } , \nu _ { k } ^ { o } , \pi _ { k } ^ { o } \rangle$ representing q model output relational data. The filter list $\pi _ { k } ^ { o }$ has

(1) clauses to support the maximum dimensional property involving variables associated with $\kappa _ { k } ^ { o }$ and $\nu _ { k } ^ { o }$ ,

(2) additional binding clauses from $\cup _ { j = 1 , . . . , p } \pi _ { j } ^ { \ i }$ and Filter such that $\pi _ { k } ^ { o } \supset \{ \cup _ { j = 1 , . . . , p } \pi _ { j } ^ { i } \} \cup$ Filter, and (3) the filter space associated with $\pi _ { k } ^ { o } , \mathrm { F S } _ { \pi k } ^ { o } \ne \ O$ ;

Assumption is a set of predicate clauses representing model assumptions that are not related to model inputs.

Filter is the set of filter clauses representing constraints among model inputs and can only contain clauses of Type 1a and clauses of Type 2 that are in turn composed of clauses of Type1a. The filter space associated with Filter, $\mathrm { F S } _ { \mathrm { F i l t e r } } { \neq } \ \emptyset$ , when AFilter $\neq 0 ;$

Source is a string that encodes the physical location and access method of the model executable.

Definition VII. Let $X _ { \pi i }$ and $X _ { \pi j }$ denote sets of unique variables appearing in filter lists $\pi _ { i }$ and $\pi _ { j } ,$ respectively. A projection of list $\pi _ { j }$ over $\pi _ { i }$ is defined by the function $\varphi _ { \pi j i : } \pi _ { j } \longrightarrow \pi _ { j i }$ as follows:

11.1 Set $\pi _ { j i } = \pi _ { j }$

11.2 Remove all clauses of Type 1b of the following form from $\pi _ { j i } { : \phi } ( x , c )$ where $x \notin X _ { \pi i }$ and $c { = } \mathrm { M I N { \_ } R A N G E }$ or MAX<sup>\_</sup>RANGE.

11.3 Replace Type 2 clauses in $\pi _ { j i }$ that has arity = 1 with its remaining Type 1 component clause. Remove all Type 2 clauses that have arity = 0.

11.4 Remove all non-binding clauses from $\pi _ { j i } .$ As an example, consider two filter lists.

Definition VIII. Let $\pi _ { i }$ and $\pi _ { j }$ denote two filter lists, and $\pi _ { j i }$ denote the filter list obtained after the projection of $\pi _ { j }$ over $\pi _ { i } .$ Furthermore, let $X _ { \pi i }$ and $X _ { \pi j i }$ denote sets of unique variables appearing in $\pi _ { i }$ and $\pi _ { j i } ,$ respectively, and $\mathrm { F S } _ { \pi i }$ and $\mathrm { F S } _ { \pi j i }$ denote the filter spaces of $\pi _ { i }$ and $\pi _ { j i } ,$ respectively. The filter list $\pi _ { i }$ is covered in $\pi _ { j }$ iff

12.1 $X _ { \pi i } = X _ { \pi j i }$ (i.e., the dimensions of $\pi _ { j }$ after projection, are the same as that of $\pi _ { i \cdot } )$

12.2 $\mathrm { F S } _ { \pi i } \subseteq \mathrm { F S } _ { \pi j i }$ (i.e., the filter space of $\pi _ { i }$ is contained in the projected filter space of $\pi _ { j } )$ .

Definition IX. Let output relation s of model m defined by $\kappa - \nu ^ { \mathrm { s } } = \langle \eta ^ { \mathrm { s } } , \kappa ^ { \mathrm { s } } , \nu ^ { \mathrm { s } } , \pi ^ { \mathrm { s } } \rangle$ provide data for a query $\kappa - \boldsymbol { v } ^ { \mathrm { q } } = \langle \eta ^ { \mathrm { q } } , \kappa ^ { \mathrm { q } } , \nu ^ { \mathrm { q } } , \pi ^ { \mathrm { q } } \rangle$ . Furthermore, let $\kappa - \nu _ { j } =$ $\langle \eta _ { j } , \kappa _ { j } , \nu _ { j } , \pi _ { j } \rangle$ denote the key-value set definition of the jth model input relation, $X _ { \pi \mathrm { q } }$ denote the set of unique variables appearing in $\pi ^ { \mathrm { q } } .$ , and $X _ { \pi j }$ denote the set of unique variables appearing in $\pi _ { j } .$ The input filter reduction of $\pi _ { j }$ over $\pi ^ { \mathrm { q } }$ is defined by the function ${ \psi _ { \pi } } ^ { \mathrm { q } } { : \pi _ { j } } \to \pi _ { j \mathrm { q } } ^ { \prime }$ as follows:

13.1 Set $\pi _ { j \mathrm { q } } ^ { \prime } = \pi _ { j }$

13.2 Replace all clauses of Type 1b of the form $\phi ( x , c 1 )$ in $\pi _ { j \mathrm { q } } ^ { \prime } .$ , where ${ x } { { \in } } X _ { \pi \mathrm { q } } \cap X _ { \pi j }$ and $c 1 { \in } C$ with clauses of the same type $\Phi ( x , c 2 )$ from $\pi ^ { \mathrm { q } }$ where $c 2 { \in } C$

13.3 Remove all clauses of Type 1b of the following form from $\pi _ { j \mathrm { q } } ^ { \prime } : \varPhi ( x , c )$ where $x \notin X _ { \pi \mathrm { q } }$ and $c = \mathrm { M I N \_ R A N \bar { G } \bar { E } }$ or MAX<sup>\_</sup>RANGE.

13.4 Add clauses $\phi ( x , y )$ from $\pi ^ { \mathrm { q } }$ such that $\Phi ( x , y ) \notin \pi _ { j \mathrm { q } } ^ { \prime }$ , and the set of variables associated with arguments x and $y , \ X _ { x }$ and $X _ { \nu } ,$ respectively, are contained in $X _ { \pi j } , \mathrm { ~ i . e . , ~ } X _ { x } \subseteq \dot { X _ { \pi j } }$ and $X _ { y } \subseteq X _ { \pi j }$

Definition X. A model template M with Input= $\{ \kappa - \nu _ { j } ^ { ~ i } \} _ { j = 1 , . . . , m } ,$ and inter-input constraint set Filter can be customized for a given query q with filter list $\pi ^ { \mathrm { q } }$ as follows:

14.1 Reduce filter list of $\kappa - \nu _ { j } ^ { ~ i } , j = 1 , \ldots , m$ (Definition IX).

14.2 Set Filter = Filter $\mathcal { S } C _ { i }$ , where $C = \{ \varPhi ( \cdot ) | \varPhi ( \cdot )$ $\begin{array} { r l } { \in \pi ^ { { \mathfrak { q } } } \backslash \exists \kappa - \nu _ { { \mathfrak { j } } } ^ { { \mathfrak { i } } } } & { { } ( X _ { \phi ( \cdot ) } \cap X _ { { \mathfrak { j } } } ^ { { \mathfrak { i } } } \neq \emptyset ) \lor \exists \kappa - \nu _ { { \mathfrak { k } } } ^ { { \mathfrak { i } } } ( X _ { \phi ( \cdot ) } } \end{array}$ $\cap X _ { k } ^ { i } \neq \emptyset ) \lor ( j \neq \ k ) \}$ , and $X _ { \phi ( \cdot ) } , ~ X _ { j } ^ { i } , ~ X _ { k } ^ { i }$ represent the set of variables in $\begin{array} { r } { \varPhi ( \cdot ) , \kappa - \nu _ { j } ^ { i } } \end{array}$ , and $\kappa - \nu _ { k } ^ { i } ,$ respectively.

## References

[1] S. Ba, K.R. Lang, A.B. Whinston, Enterprise decision support using intranet technology, Decision Support Systems 20 (2) (1997) 99 – 134.

[2] A. Basu, R.W. Blanning, Model integration using metagraphs, Information Systems Research 5 (3) (1994) 195 – 218.

[3] A. Basu, R.W. Blanning, The analysis of assumptions in model bases using metagraphs, Management Science 44 (7) (1998) 982– 995.

[4] E. Bertino, M. Negri, G. Pelagatti, L. Sbattella, Applications of object-oriented technology to the integration of heterogeneous database systems, Distributed and Parallel Databases 2 (1994) 343– 370.

[5] H.K. Bhargava, R. Krishnan, R. Muller, Decision support on demand: emerging electronic markets for decision technologies, Decision Support Systems 19 (3) (1997) 193 – 214.

[6] R.W. Blanning, A relational framework for join implementation in model management systems, Decision Support Systems 1 (1) (January 1985) 69 – 81.

[7] R.H. Bonczek, C.W. Holsapple, A.B. Whinston, Foundations of Decision Support Systems, Academic Press, New York, 1981.

[8] K. Chari, ‘‘An Enterprise-Wide Decision Support System,’’ Technical Report, Department of Information Systems and Decision Sciences, University of South Florida, Tampa, FL 33620, 2001.

[9] K. Chari, Model composition using filter spaces, Information Systems Research 13 (1) (2002) 15– 35.

[10] D. Comer, Internetworking with TCP/IP: Principles, Protocols, and Architecture, 4th edn., Prentice-Hall, Upper Saddle River, NJ 07458, 2000.

[11] A. Datta, H. Thomas, The cube data model: a conceptual model and algebra for on-line analytical processing in data warehouses, Decision Support Systems 27 (1999) 289 – 301.

[12] A. Dutta, A. Basu, An artificial intelligence approach to model management in decision support systems, IEEE Computer 17 (9) (September 1984) 89 – 97.

[13] M. Holocher, R. Michalski, D. Solte, F. Vicuna, MIDA: an open systems architecture for model-oriented integration of data and algorithms, Decision Support Systems 20 (2) (1997) 135– 147.

[14] M.A. Jeusfeld, T.X. Bui, Distributed decision support and organizational connectivity, Decision Support Systems 19 (3) (1997) 215–225.

[15] J.E. Kottemann, D.R. Dolk, Model integration and modeling languages: a process perspective, Information Systems Research 3 (1) (1992) 1 – 16.

[16] R. Krishnan, K. Chari, Model management: survey, future research directions and a bibliography, Interactive Transactions of OR/MS 3 (1) (2000).

[17] R. Krishnan, P. Piela, A. Westerberg, Reusing mathematical models in ASCEND, in: C. Holsapple, A. Whinston (Eds.), NATO ASI on Decision Support Systems, Springer, New York, NY, 1993, pp. 275 – 294.

[18] T.P. Liang, Development of a knowledge-based model management system, Operations Research 36 (6) (1988) 849 – 863.

[19] L. Liu, C. Pu, An adaptive object-oriented approach to integration and access of heterogeneous information sources, Distributed and Parallel Databases 5 (1997) 167 – 205.

[20] L.M. Mackinnon, D.H. Marwick, M.H. Williams, A model for query decomposition and answer construction in heterogeneous distributed database systems, Journal of Intelligent Information Systems 11 (1998) 69– 87.

[21] M.K. Mayer, Future trends in model management systems: parallel and distributed extensions, Decision Support Systems 22 (1998) 325– 335.

[22] V.S. Mookerjee, A.R. Chaturvedi, A blackboard control architecture for model selection and sequencing, European Journal of Information Systems 2 (1) (1993) 3 – 14.

[23] W.A. Muhanna, R.A. Pick, Meta-modeling concepts and tools for model management: a systems approach, Management Science 40 (9) (1994) 1093 – 1123.

[24] E. Rich, K. Knight, Artificial Intelligence, 2nd edn., McGraw-Hill, New York, NY 10020, 1991.

[25] M.J. Shaw, P.L. Tu, P. De, Applying machine learning to model management in decision support systems, Decision Support Systems 4 (1988) 285– 305.

[26] A.P. Sheth, J.A. Larson, Federated database systems for managing distributed, heterogeneous, and autonomous databases, ACM Computing Surveys 22 (3) (1990) 183 – 236.

[27] R.H. Sprague Jr., E.D. Carlson, Building Effective Decision Support Systems, Prentice-Hall, Englewood Cliffs, NJ, 1982.

[28] A. Umar, Object-Oriented Client/Server Internet Environments, Prentice-Hall, Upper Saddle River, NJ 07458, 1997.

[29] N. Wang, Y. Chen, B. Yu, N. Wang, Versatile: a scalable CORBA-based system for integrating distributed data, Proceedings of IEEE International Conference on Intelligent Processing Systems, Beijing, China, 1997, pp. 1589 – 1593.

[30] H.J. Will, Model management systems, in: E. Grochla, N. Szyperski (Eds.), Information Systems and Organization Structure, Walter de Gruyter, Berlin, 1975, pp. 468 – 482.

[31] X. Wu, Query processing and transforming for a federated database system, Proceedings of 8th DEXA, Toulouse, France, 1997, pp. 593–602.

[32] X. Zhang, J. Liu, H. Li, Object-oriented integration of heterogeneous distributed databases, Proceedings of IEEE TEN-CON, Beijing, 1993, pp. 331– 334.

![](/api/attachments/99M75GUN/fulltext/images/3e04a8d4dcda15e4f7f229980c6c522037f48fb91a1a269d6f6364762de5971b.jpg)

Kaushal Chari is an Associate Professor of Information Systems and Decision Sciences at the University of South Florida. He obtained a B.Tech. in Mechanical Engineering from the Indian Institute of Technology Kanpur, followed by an MBA and PhD from the University of Iowa. His current research interests include DSS, ecommerce, workflow systems and multiagent systems. His past research appeared in journals such as Decision Support Sys-

tems, INFORMS Journal on Computing, Information Systems Research, Telecommunication Systems, European Journal of Operational Research, Computers and Operations Research and Omega. He was the Co-Chair of INFORMS CIST 2001 and is currently serving as the Functional Editor-MIS for Interfaces Journal.
