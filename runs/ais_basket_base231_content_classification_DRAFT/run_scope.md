# 草案范围——用户确认前禁止运行

- 输入范围：上一轮 `base_match=true` 的 231 篇文章。
- 110 篇 `theory_guided_subset_match=true` 文章不重复请求；理论子集标签将在模型分类后从上一轮结果本地连接。
- 每篇完整全文一个独立请求，不把多篇文章放进同一任务。
- 模型计划：`deepseek-v4-flash`。
- 本轮只做内容概述和三个正交维度的分类：软件主功能、主要客观结果、应用情境。
- 不重新筛选文章，不重新判断理论是否合格，也不做 coding-agent 迁移。
- 计划汇总：各类别数量与占比、类别×期刊、类别×年份、理论子集与非理论子集对比，并单列 ISR。
- 当前没有创建 API 配置、任务队列或结果目录，也没有调用模型。

