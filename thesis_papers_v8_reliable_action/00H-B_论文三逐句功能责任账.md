# 论文三逐句功能责任账

## 1. 状态与适用范围

统一状态：**REPAIRED-v5 / INDEPENDENT RE-REDTEAM PENDING / RESULTS NOT RUN**。

本账只审计 `00J` 第 12 节的 18 组关键正式候选句卡 `C3-K01–C3-K18`。K03、K09、K11、K12、K14 和 K15 含拆分后的多个语法句，但仍各保留一个句卡 ID 与一个用于 18 卡统计的法定 RESULT。K14 依 `02T` 另为六个语法句各给一行完整责任链和句内 RESULT，不以聚合链代替逐句责任。本账不能替代未来完整正文的逐句和逐段审计，也不表示任何实验已经运行。旧 `00J` 第 9–11 节段落蓝图已整体作废；未列入本账的旧蓝图不得因位置相似被继续使用。修补者无权自行判定 `DESIGN-FROZEN`。

每张卡都使用 `00W` 的段落主键与语法句 ID，并分别填写 `source_function_code` 与 `candidate_function_code`。主键可解析只证明源句存在，不证明功能同位。`RESULT` 只允许“保留、改写、拆分、合并、删除”。候选句学习的是肖老师论文的句子责任与推进关系，不近似改写其文字。

## 2. 逐句责任卡

### C3-K01 持久状态行动情境

- 候选句：Coding agent 不仅生成代码建议，还会通过补丁、命令和工具调用改变仓库及其运行环境的持久状态。
- 原文参照：`ACAA-I1 / ACAA-I1-S01`。
- `source_function_code=CXT`；`candidate_function_code=CXT`。
- 同位判定：功能同位。两句都先建立真实信息或行动渠道，不提前暴露理论和算法。
- `PRE=读者尚不知道研究发生在哪个工作流 → SRC_NEW=在线评论是顾客与产品之间的日常信息渠道 / CAND_NEW=coding agent 通过补丁、命令和工具调用改变持久状态 → REL=情境起点同位 → CIT=源句的 Yin et al. 只支持评论渠道；候选句需 coding-agent 实际行动的一手实证，不能只引产品文档 → NEXT=说明行动错误为何影响组织结果 → RESULT=保留`。

### C3-K02 外部损害重要性

- 候选句：当组织把这类行动交由开发者批准时，误批可能使功能破坏、越界修改或不可恢复的状态变化进入后续软件过程。
- 原文参照：`ACAA-I1 / ACAA-I1-S04`。
- `source_function_code=IMP`；`candidate_function_code=IMP`。
- 同位判定：功能同位，结果对象不同。
- `PRE=已建立 agent 能改变持久状态 → SRC_NEW=sales prediction 影响运营资源配置与价值 / CAND_NEW=误批把功能、范围或状态损害带入后续软件过程 → REL=行动情境推进为外部重要性 → CIT=候选句需要事故、开发者实践或监督实证；benchmark failure rate 不能独立支持组织损害规模 → NEXT=引入组织设置批准点的朴素方案 → RESULT=保留`。

### C3-K03 承认 action binding 已解决的部分

- 候选句 1：在可信中介和完整拦截成立的范围内，行动绑定使显示对象与执行对象保持一致。
- 候选句 2：这一属性并不说明审核者已经正确解释行动后果。
- 原文参照：句 1 参照 `ACAA-I2 / ACAA-I2-S01`；句 2 为**无同功能锚点**。
- 句 1：`source_function_code=ACK`；`candidate_function_code=ACK`；同位判定为**功能同位**。两句都先承认直接方案在明确前提内已经解决的能力，再为下一句留下未解边界。
- 句 2：`source_function_code=NONE`；`candidate_function_code=LIM`；同位判定为**无同功能锚点**。ACAA-I2-S01 没有同时承担人的解释边界，不能让一个 ACK 源句覆盖新增 LIM。
- `PRE=人工批准已成为控制点 → SRC_NEW=ACAA 由既知 review–sales 关系提出全量输入的直接方案 / CAND_NEW=句1在 trusted mediation/complete interception 条件内承认显示—执行一致，句2再限定该属性不推出人的正确解释 → REL=ACK 后接 LIM，两个学术动作拆为两个语法句 → CIT=Consent Integrity 只在其 trusted path/total mediation 假设内支持 binding 并明确不处理 comprehension；句2还需真人审核或人机理解直接证据，不由 ACAA 支持 → NEXT=区分 evidence validity 与 understanding → RESULT=拆分`。

### C3-K04 可见证据与理解状态的区分

- 候选句：可执行证据能够改善技术事实的可核验性，但证据已经展示与审核者已经形成 action-specific understanding 仍是两个不同状态。
- 原文参照：`ACAA-I3 / ACAA-I3-S02`。
- `source_function_code=MEC`；`candidate_function_code=MEC`。
- 同位判定：功能同位。源句为评论影响加入认真阅读条件，候选句为技术证据影响加入正确解释条件。
- `PRE=行动已绑定且证据可执行 → SRC_NEW=评论影响购买还要求顾客认真阅读 / CAND_NEW=证据可核验仍不等于审核者已形成行动特定理解 → REL=输入到结果之间加入必要处理状态 → CIT=技术文献支持 evidence validity，common-ground 原文支持处理区分；coding approval 中的实际路径仍须真人 pilot → NEXT=引入 potential 与 established 的理论机制 → RESULT=保留`。

### C3-K05 potential 到 established 的机制

- 候选句：共同基础理论据此提示，行动相关事实从双方可访问的 potential common ground 转为 established common ground，需要针对当前目的取得可观察的相互理解证据。
- 原文参照：`DSDL-I6 / DSDL-I6-S04`。
- `source_function_code=MEC`；`candidate_function_code=MEC`。
- 同位判定：功能同位，但理论内容不同。两句都在既有可观察状态之外加入不可省略的第二状态。
- `PRE=证据展示与理解建立已经被区分 → SRC_NEW=体验之外还需要期望状态 / CAND_NEW=potential access 之外还需要 purpose-relative mutual-understanding evidence 才能形成 established state → REL=遗漏状态补入因果链 → CIT=Mastrogiacomo 支撑 criterion，Li/Poelitz 支撑 potential/established 与互动证据；均不直接证明 coding harm → NEXT=精确说明理论条件与内容类别的关系 → RESULT=保留`。

### C3-K06 修正 joint-purpose 映射

- 候选句：Mastrogiacomo 等人的项目转译把 identification 连到 objective，把 ability 同时连到 resource 与 risk，并把 willingness 连到 commitment。
- 原文参照：`ACAA-I5 / ACAA-I5-S01`。
- `source_function_code=MAP`；`candidate_function_code=MAP`。
- 同位判定：**位置参照，非功能同位**。ACAA 源句建立同一理论现象的 temporal/spatial 两维；候选句核对另一理论原文中的稀疏关系。二者虽都可编码为 MAP，producer、关系操作、引文责任与下一步均不同。
- `PRE=共同基础机制已进入研究 → SRC_NEW=以 temporal 与 spatial 两维组织浏览行为 / CAND_NEW=按 Mastrogiacomo 原文建立 identification→objective、ability→resource+risk、willingness→commitment 的非一一映射 → REL=只借鉴理论映射出现的位置，不借用源句的功能证明 → CIT=候选映射只能由 Mastrogiacomo Table 3 支持；肖文不承担该关系 → NEXT=说明 mutual belief 不属于内容轴 → RESULT=保留`。

### C3-K07 mutual belief 的过程责任

- 候选句：mutual belief 并非另一类任务内容，而是在持续对话中围绕这些内容寻找、检验和修复相互理解证据的过程。
- 原文参照：`ACAA-I5 / ACAA-I5-S03`。
- `source_function_code=MEC`；`candidate_function_code=MEC`。
- 同位判定：**位置参照，非功能同位**。源句从 process dimension 导入 message learning 与 information processing；候选句纠正 mutual belief 的本体位置。两句的 producer 与论证动作不同。
- `PRE=前三项条件与四类内容的关系已经修正 → SRC_NEW=过程维由 message learning 与 information processing 构成 / CAND_NEW=mutual belief 由围绕内容的持续对话、检验与 repair 承担 → REL=只借鉴内容结构后进入过程讨论的位置，不能声称同一推进责任 → CIT=Mastrogiacomo 的会议/对话与 grounding criterion 支撑候选定位；ACAA 不支持该构念解释，也不支持具体张量或门函数 → NEXT=把四类内容写成外生 manifest → RESULT=保留`。

### C3-K08 外生 `J_content`

- 候选句：因此，本研究在 immutable action binding 完成后，由任务作者从 task/action manifest 冻结 objective、resource、risk 与 commitment 的外生内容矩阵 `J_content`，而不把它作为理论模型的生成结果。
- 原文参照：`DSDL-M15 / DSDL-M15-S04`。
- `source_function_code=ALG`；`candidate_function_code=MAP+BND`。
- 同位判定：**位置参照，非功能同位**。DSDL 源句以可学习投影生成动态期望矩阵；候选句在模型之外冻结内容矩阵并禁止模型生成。producer 与运算方向相反。
- `PRE=理论条件与内容/过程层已区分 → SRC_NEW=可学权重把偏好模板投影为动态期望矩阵 / CAND_NEW=immutable binding 后由 task author 冻结 J_content，模型只消费而不生成 → REL=只借鉴方法段引入具名矩阵的位置，不能借用学习型投影的功能责任 → CIT=矩阵定义、bind-first 和外生性是本文识别合同；内容关系回指 Mastrogiacomo 与 task/action manifest，DSDL 不支持该冻结规则 → NEXT=逐一界定可学习 interpretation 与过程状态 → RESULT=保留`。

### C3-K09 中间状态语义

- 候选句 1：模型以 `V_t` 表示行动命题对人和 agent 的实际可访问性。
- 候选句 2：`V_t` 只由 renderer access 与权限日志产生。
- 候选句 3：模型以 `B_H,t` 表示审核者对 action-specific facts 的解释后验。
- 候选句 4：`B_H,t` 只由独立 factual response 与可信 public execution fact 校准。
- 候选句 5：`C_t` 记录 presentation、clarification、repair 与 `grounding_acceptance`。
- 候选句 6：`C_t` 的标签来自不读取审批结果的盲态话轮标注。
- 候选句 7：`M_t` 记录独立的人类理解证据与审核者可观察的 system uptake evidence。
- 候选句 8：`M_t` 的 event ID 不得复用 `C_t` 的 response event。
- 候选句 9：这些状态以可证伪的结构使用方式形成 purpose-relative established-state estimate `g_est,t`。
- 原文参照：句 1、3、5、7 只以 `DSDL-M15 / DSDL-M15-S05` 作为状态语义出现位置的参照；句 2、4、6、8 为**无同功能锚点**；句 9 参照 `ACAA-M10b / ACAA-M10b-S05`。
- `source_function_code=[句1 MAP；句2 NONE；句3 MAP；句4 NONE；句5 MAP；句6 NONE；句7 MAP；句8 NONE；句9 ALG]`；`candidate_function_code=[句1 MAP；句2 BND；句3 MAP；句4 BND；句5 MAP；句6 BND；句7 MAP；句8 BND；句9 ALG]`。
- 同位判定：句 1、3、5、7 为**位置参照，非功能同位**。DSDL 源句解释一个既定元素 `E_ij` 的经验语义，四个候选句则分别界定 access、interpretation、grounding act 与 reciprocal uptake，producer、观测对象和后续识别责任均不同。句 2、4、6、8 是防标签循环与事件复用的 provenance 边界，均为**无同功能锚点**。句 9 与 ACAA-M10b-S05 在“让前述理论诊断实际改变前向计算”这一 ALG 责任上功能同位，但不借用源文的具体 compatibility calculation。
- `PRE=J_content 已成为外生输入，但四个理论状态的含义、来源和聚合责任尚未分开 → SRC_NEW=DSDL 解释单个客户×属性×时点元素的语义，ACAA 让理论诊断 r 进入 compatibility calculation / CAND_NEW=句1/3/5/7分别定义 V/B_H/C/M 的对象，句2/4/6/8分别冻结互斥的可观察 producer，句9才把这些状态用于 g_est → REL=状态语义只作位置参照，provenance 边界无锚点，最后的 ALG 责任单独对照 → CIT=V/B_H/C/M 的理论含义分别回指 Mastrogiacomo、Li、Poelitz 与 Tolzin/Janson；producer 合同只由 00H-A 的字段、角色与时间戳证明；g_est 的组合形式是本文可证伪结构假设，DSDL 不承担其正确性 → NEXT=冻结 purpose-relative criterion 并阻断 outcome 反推 → RESULT=拆分`。

### C3-K10 ex ante criterion 防循环

- 候选句：批准所需的命题与阈值 `m_req/κ` 在任何审核者回答、批准和 hidden outcome 出现之前冻结，使最终结果不能循环定义共同理解。
- 原文参照：`DSDL-M19 / DSDL-M19-S01`。
- `source_function_code=LIM→MAP`；`candidate_function_code=MAP+BND`。
- 同位判定：位置参照，非功能同位。源句回应无真值问题的动作是寻找 proxy supervision，候选句的动作是禁止 outcome 反推并事前冻结 criterion，推进方向不同。
- `PRE=理论状态已定义但结果循环仍需封闭 → SRC_NEW=E/F 没有直接真值，转用理论关系提供弱监督 / CAND_NEW=m_req/κ 在回答、批准与 hidden outcome 前冻结 → REL=两句都回应识别困难，但源句寻找 proxy，候选句禁止后验回填，故只有位置参照 → CIT=purpose-relative criterion 由共同基础原文支持；冻结时点、scale/basis/unit/bank hash 是本文识别设计，不由 DSDL 证明 → NEXT=说明门函数只是可证伪结构假设 → RESULT=保留`。

### C3-K11 门函数责任降格

- 候选句 1：四路门将上述必要路径写入前向计算。
- 候选句 2：乘性形式只是一项理论启发的结构假设，其经验价值需要通过校准与干预检验。
- 原文参照：句 1 参照 `ACAA-M10b / ACAA-M10b-S05`；句 2 为**无同功能锚点**。
- `source_function_code=[句1 ALG；句2 NONE]`；`candidate_function_code=[句1 ALG；句2 BND]`。
- 同位判定：句 1 与源句在“让前述理论诊断进入前向计算”这一 ALG 责任上功能同位，不借用其具体 compatibility 运算。句 2 明确乘性形式的经验待验边界，源句没有承担这一 BND 责任，故为**无同功能锚点**。
- `PRE=四条必要证据路径已定义，但尚未说明它们是否实际改变模型 → SRC_NEW=ACAA 以理论诊断 r 修正 compatibility calculation / CAND_NEW=句1让四路必要路径进入前向，句2把乘性形式降为待校准、待干预检验的结构假设 → REL=ALG 与新增 BND 分句承担，不能由一个源句同时背书 → CIT=Mastrogiacomo 只支持必要的理解证据与 purpose-relative sufficiency，不支持乘法、平方根或特定单调形式；结构价值须由校准、机制中和和通用替代实验证明 → NEXT=说明结构状态改变哪些序贯动作 → RESULT=拆分`。

### C3-K12 固定 post-escalation 动作边界

- 候选句 1：在外部机制已经决定升级之后，策略只选择下一项 `acquire`、`present`、`factual_probe`、`clarify`、`repair` 或 `stop`。
- 候选句 2：模型不决定是否启动人工审核，也不输出批准、拒绝、风险分数或审核者分配。
- 原文参照：句 1 参照 `DSDL-M23 / DSDL-M23-S01`；句 2 为**无同功能锚点**。
- 句 1：`source_function_code=ALG`；`candidate_function_code=ALG`；同位判定为**位置参照，非功能同位**。源句闭合联合损失，候选句定义 post-escalation 的六类合法动作，输出对象与逻辑操作不同。
- 句 2：`source_function_code=NONE`；`candidate_function_code=BND`；同位判定为**无同功能锚点**。源句没有排除启动审核、批准、拒绝、风险评分或人员分配的边界责任，不能由句 1 的位置参照代为承担。
- `PRE=状态、结构门与 residual gap 已定义 → SRC_NEW=DSDL-M23-S01 把局部损失闭合为整体优化，且没有承担相邻研究排除边界 / CAND_NEW=句1把 gap 转为六类合法动作，句2另行排除启动审核、批准、拒绝、风险分数与人员分配 → REL=句1只有章节位置的“从局部计算走向输出”相似而非功能同位，句2无同功能锚点 → CIT=六类动作是本文方法；Hedwig/CDCR/PCAA/C19 只界定被排除动作，`询问时点` 已删除以避免与 factual_probe 冲突；DSDL-M23-S01 不支持两项候选主张 → NEXT=为六类动作建立 oracle-blind 候选时序 → RESULT=拆分`。

### C3-K13 oracle-blind lattice

- 候选句：为使取证动作不携带隐藏答案，候选动作、mask、成本、超时与顺序在 hidden evaluator 运行之前由仅接触部署可见字段的 generator 写入带 hash 的 lattice manifest。
- 原文参照：`DSDL-E1 / DSDL-E1-S09`。
- `source_function_code=EVAL`；`candidate_function_code=EVAL+BND`。
- 同位判定：位置参照，非功能同位。源句只交代样本时间边界，肖老师两篇正文中没有 oracle-before-lattice 的同功能句。
- `PRE=合法动作已经定义但候选可用性仍可能泄漏 hidden outcome → SRC_NEW=报告样本观察时间边界 / CAND_NEW=规定 lattice hash 必须早于 hidden evaluator 并冻结 side-channel 字段 → REL=都建立评价时序，但候选句承担更强的隔离责任 → CIT=该规则由 02E 的 oracle red-team 与 BSG-VA/benchmark 隔离需求支持，不由肖文位置支持 → NEXT=说明用于学习和验证的真人 cohort 如何隔离 → RESULT=保留`。

### C3-K14 cohort 隔离、固定臂与参与者簇分析

- 候选句 1：测量 pilot、策略学习 logging cohort 与前瞻检验 cohort 使用互不复用的参与者，且只有 logging cohort 的已知 propensity 轨迹用于更新策略。
- 候选句 2：前瞻检验按处理前 `baseline_reviewer_support` 对 `participant_id` 分层，并以预提交 HMAC 映射把每名参与者一次性分到概率均为四分之一的 `A/B/C/D` 四臂。
- 候选句 3：同一参与者的十二个计划 trial 全程保持同臂，D/C 确认性对比的条件处理概率均为二分之一。
- 候选句 4：分臂 manifest 在首例入组前冻结域字符串、`seed_hash`、HMAC 输入规范、字节序和 `0→A,1→B,2→C,3→D` 映射，并在 enrollment lock 后公开 key 以供重放。
- 候选句 5：机制分析把参与者 cluster 作为 IID 观测单位，在簇内纵向 likelihood 中显式纳入十二 trial 的 carryover，并仅假定不同参与者之间无干扰。
- 候选句 6：shared-support 目标总体以参与者等权且人内十二 trial 等权，五折 cross-fitting 与 2,000 次 bootstrap 均以完整 participant cluster 为不可拆分单位。
- 原文参照：句 1 参照 `DSDL-E1 / DSDL-E1-S04`；句 2–6 均为**无同功能锚点**。
- `source_function_code=[句1 EVAL；句2 NONE；句3 NONE；句4 NONE；句5 NONE；句6 NONE]`；`candidate_function_code=[句1 EVAL；句2 BND；句3 BND；句4 BND；句5 MAP+BND；句6 EVAL+BND]`。
- 同位判定：句 1 为**位置参照，非功能同位**。DSDL 源句通过排除历史处理界定样本，候选句 1 通过参与者跨 cohort 不复用和训练数据单向流动保护前瞻评价，两者的 producer 和识别威胁不同。句 2–4 冻结 participant-level 分层四臂、arm persistence 和可重放 HMAC 合同，句 5–6 冻结 IID 单位、carryover、目标总体和整簇推断，肖老师两篇正文没有承担这些实验 BND/EVAL 责任，故均诚实登记为无锚点。

| 句 | PRE | SRC_NEW | CAND_NEW | REL | CIT | NEXT | 句内 RESULT |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | oracle 与 cohort 隔离已封闭技术标签泄漏，但策略学习与前瞻评价仍可复用同一参与者 | `DSDL-E1-S04` 排除曾接受同类处理的客户 | 三 cohort 参与者互不复用，只有 logging propensity 轨迹更新策略 | 仅为样本排除的位置参照，producer 和识别威胁不同 | participant registry、cohort manifest 与 training-input audit 证明候选句；DSDL 只证明其自身样本界定 | 冻结 confirmatory participant 的分臂律 | 拆分 |
| 2 | cohort 已分开，但 confirmatory treatment 仍可按 trial 或 participant 分配 | `SRC_NEW=NONE` | 按处理前 support 对 `participant_id` 分层，以预提交 HMAC 一次性映射到每臂概率 `1/4` 的四臂 | 肖文无同功能随机化 BND 锚点 | arm-randomization protocol、pre-enrollment hash 与 assignment manifest 证明 | 界定十二 trial 的 arm persistence 与 D/C 子对比 | 拆分 |
| 3 | participant-level 初始分配已定，但后续 trial 仍可 crossover、重随机或因缺席换臂 | `SRC_NEW=NONE` | 每人十二个计划 trial 全程同臂，D/C 子对比的已知条件概率均为 `1/2` | 肖文无同功能 arm-persistence BND 锚点 | assignment 外键、十二 trial 一致性门与 D/C analysis manifest 证明 | 使分配对实施者隐藏且事后可重放 | 拆分 |
| 4 | 处理律已定，但 seed、输入规范或映射仍可被人工改动 | `SRC_NEW=NONE` | 首例前冻结域、`seed_hash`、HMAC 输入、字节序与四臂映射，enrollment lock 后公开 key | 肖文无同功能可重放分配 BND 锚点 | pre-enrollment manifest、公开 key 与两个独立实现的 HMAC/arm 重放证明 | 把多 trial 观测的独立单位与 carryover 写入 likelihood | 拆分 |
| 5 | 参与者处理已唯一，但十二 trial 仍可被当成 IID 行并排除簇内学习 | `SRC_NEW=NONE` | 以 participant cluster 为 IID 单位，在簇内纵向 likelihood 中纳入 carryover，no-interference 仅限跨参与者 | 肖文无同功能聚类 likelihood BND/MAP 锚点 | cluster-likelihood schema 与全历史重放证明；Wang 等人只支持纵向方法，不原样规定本实施 | 冻结 participant-equal estimand 与整簇推断 | 拆分 |
| 6 | IID 与 carryover 已定，但目标权重、fold 和 bootstrap 仍可以 trial 行为单位 | `SRC_NEW=NONE` | shared-support 参与者等权、人内十二 trial 等权，五折与 2,000 次 bootstrap 都抽不可拆的完整 participant cluster | 肖文无同功能 estimand/cross-fit/resampling BND/EVAL 锚点 | estimand、participant-to-fold 与 cluster-bootstrap manifest 及失败日志证明 | 以 shared-support 门和失败规则限定机制结论 | 拆分 |

- `PRE=oracle 与 cohort 隔离已封闭技术标签和跨阶段污染，但随机化单位、十二 trial 的处理维持、簇内 carryover 与目标权重仍可被实现为两种不同实验 → SRC_NEW=DSDL-E1-S04 排除曾接受同类处理的客户，只承担其自身样本边界 → CAND_NEW=句1分开三 cohort 与训练流，句2–4把 confirmatory 处理唯一化为可重放的 participant-level 固定四臂，句5–6把 participant cluster 唯一化为 likelihood、carryover、estimand、fold 与 bootstrap 单位 → REL=句1只借鉴样本排除条件的位置；五个新语法句各自承担无源句背书的方法边界 → CIT=句1由 participant registry、cohort manifest 和 training-input audit 证明；句2–4只能由 arm-randomization protocol、pre-enrollment hash 与公开 key 重放证明；句5–6由 cluster likelihood/estimand/bootstrap manifest 与统计输出证明。Wang 等人只支持纵向随机干预方法，不原样规定固定臂或聚类重抽；DSDL 不支持句2–6 → NEXT=说明通用基线得到完全相同的信息 → RESULT=拆分`。

### C3-K15 等信息通用替代

- 候选句 1：研究比较 Human-state POMDP、Raw-POMDP、generic proposition Transformer 与 contextual bandit 四类通用替代。
- 候选句 2：这些替代获得相同内容矩阵、命题标签、人类反馈、候选 lattice 与动态分支实例、回报、参数、FLOPs 和搜索预算。
- 原文参照：句 1 参照 `DSDL-E3 / DSDL-E3-S02`；句 2 为**无同功能锚点**。
- `source_function_code=[句1 EVAL；句2 NONE]`；`candidate_function_code=[句1 EVAL；句2 BND]`。
- 同位判定：句 1 在“声明有竞争力的直接与通用基线比较”这一 EVAL 责任上与源句功能同位。句 2 冻结完全等信息与等资源合同，`DSDL-E3-S02` 没有这一 BND 责任，故为**无同功能锚点**。
- `PRE=训练 cohort 和标签可见性已界定，但理论结构仍可能从额外字段或预算获益 → SRC_NEW=DSDL 声明与 12 个先进深度方法比较 / CAND_NEW=句1列出四类通用替代，句2单独要求相同内容、标签、反馈、lattice/动态分支、回报与计算预算 → REL=基线比较 EVAL 与公平合同 BND 拆开，后者不强配 DSDL 锚点 → CIT=模型名称与能力边界由各方法原文支持；“相同”只能由 manifest、config 与 run log 证明，肖文句子不承担该事实 → NEXT=承认通用模型可复制表达并冻结撤销条件 → RESULT=拆分`。

### C3-K16 表达可复制与条件贡献

- 候选句：通用模型在表达能力上可以复制结构门，因此理论贡献只能由有限数据表现、未见仓库与审核者上的泛化、方向性干预响应和安全—效率前沿共同检验。
- 原文参照：`ACAA-M10a / ACAA-M10a-S01`。
- `source_function_code=ACK+LIM`；`candidate_function_code=ACK+BND`。
- 同位判定：位置参照，非功能同位。源句承认 common attention baseline 后指出其计算方向不同；候选句反而明确承认通用模型具备相同表达能力。
- `PRE=公平通用替代已获得所有相同信息 → SRC_NEW=建立通用 attention baseline 并限定其与目标的差异 / CAND_NEW=承认表达可复制，把贡献责任转向样本效率、OOD、干预和前沿 → REL=都以通用替代限定专门方法主张，但方向不同 → CIT=表达复制是模型类分析，不伪装成外部文献结论 → NEXT=给出外部主要与成本结果 → RESULT=保留`。

### C3-K17 安全与成本联合评价

- 候选句：前瞻试验以 unsafe approval 为主要结果，同时报告 safe approval、审核时间、交互次数和计算成本，并在预注册约束下比较安全—效率前沿。
- 原文参照：`DSDL-E4 / DSDL-E4-S02`。
- `source_function_code=EVAL`；`candidate_function_code=EVAL`。
- 同位判定：功能同位。源句区分主指标与互补指标，候选句区分主要安全结果、任务/成本结果与预注册前沿比较。“匹配结果”已删除，不把尚不可知的 outcome 写成可事前匹配。
- `PRE=方法与公平基线已冻结 → SRC_NEW=规定 AUC 主指标及 KS/H-measure 稳健指标 / CAND_NEW=规定 unsafe approval 主结果，同时报告 safe approval、时间、互动和 compute，并在预注册约束下比较前沿 → REL=都把研究责任转成主指标与互补指标 → CIT=participant approval 与 sealed expert truth 联合定义 primary outcome，统计预注册定义前沿；共同基础理论不承担效应大小 → NEXT=声明所有结果尚未运行 → RESULT=改写`。

### C3-K18 未运行结果边界

- 候选句：所有模型效果、机制中介与比较结果将在真实实验完成后报告，当前稿件只保留研究设计、预注册检验和结果占位。
- 原文参照：`DSDL-A0 / DSDL-A0-S05–S07`。
- `source_function_code=RES→CONB`；`candidate_function_code=BND`。
- 同位判定：**无同功能锚点**。DSDL 源句报告已有性能、机制和洞见结果；C3 目前只能约束未来写法，不能把源句的位置或完成时功能借来使用。
- `PRE=评价终点和检验已经定义 → SRC_NEW=报告已完成实验的总体、机制和解释结果 / CAND_NEW=明确结果未运行并只保留设计、预注册与占位 → REL=源文有证据而候选无证据，功能不可能同位 → CIT=当前无结果日志、表格或统计脚本，因此不得写完成时效果 → NEXT=实验完成后为每条真实结果另建 RES 句卡 → RESULT=保留`。

## 3. 段落形态责任

`C3-K01–K18` 目前是跨引言、理论、方法和实验节的关键句集合，不是连续正式段落。因尚未确定每句前后的全部正文，本账不伪造段落句数、字符数、引文数或标点配额。正式组段后，每一段必须另建 paragraph card，记录正文句数、字符数、引文标记数和冒号、分号、破折号、括号计数，并与功能最近的 `00W` 源段比较。

旧 `00J` 的 I1–I8、T1–T7、A1–A9 表格没有完成这一责任，已标记 `VOID`。本账只证明 18 个关键候选句具有句级责任链，不得扩张为“三个章节已完成逐段 ISR 对照”。

## 4. 当前机械责任

- 依 `02M`、`02P` 与 `02T` 的累积修补，18 卡法定结果必须为保留 11、改写 1（K17）、拆分 6（K03、K09、K11、K12、K14、K15）、合并 0、删除 0。
- 18 张卡必须为每个语法句分别给出 `source_function_code` 与 `candidate_function_code`；多句卡可在一对聚合字段内逐句枚举，不能让一个源句默示承担新增边界责任。K14 的六个新语法句还必须各有独立 `PRE/SRC_NEW/CAND_NEW/REL/CIT/NEXT/句内 RESULT` 行。
- 18 张卡必须各包含一次 `PRE`、`SRC_NEW`、`CAND_NEW`、`REL`、`CIT`、`NEXT` 和法定 `RESULT`。
- 所有 source key 必须落在 `00W` 登记范围；主键存在不改变“位置参照”或“无同功能锚点”的判定。
- `C3-K01–K18` 必须与 `00J` 第 12 节逐字一致；任何正式改句都要同步重做对应卡，不能只更新句子。
- 当前没有结果句、效果数字或显著性主张。

本账状态保持 **REPAIRED-v5 / INDEPENDENT RE-REDTEAM PENDING / RESULTS NOT RUN**，等待独立再红队。
